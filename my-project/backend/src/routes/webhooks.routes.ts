import type { Router } from "express";
import { createPlaceholderRouter } from "./_placeholder.js";

const webhooksRouter: Router = createPlaceholderRouter("Webhooks");

export default webhooksRouter;
