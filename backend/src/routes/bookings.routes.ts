import type { Router } from "express";
import { createPlaceholderRouter } from "./_placeholder.js";

const bookingsRouter: Router = createPlaceholderRouter("Bookings");

export default bookingsRouter;
