import type { Router } from "express";
import { createPlaceholderRouter } from "./_placeholder.js";

const packagesRouter: Router = createPlaceholderRouter("Packages");

export default packagesRouter;
